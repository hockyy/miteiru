{
  "homepage": "https://miteiru.hocky.id",
  "linux": {
    "icon": "resources/icon.icns",
    "target": ["deb", "AppImage"],
    "artifactName": "miteiru-${version}-${os}-22.04-${arch}.${ext}"
  }
}
