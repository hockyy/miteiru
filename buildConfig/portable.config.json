{
  "win": {
    "icon": "resources/icon.ico",
    "target": ["portable"],
    "artifactName": "miteiru-${version}-${os}-portable-${arch}.${ext}",
    "extraResources": [
      {
        "from": "resources/live-captions",
        "to": "live-captions",
        "filter": ["**/*", "!**/.gitkeep"]
      }
    ]
  }
}
