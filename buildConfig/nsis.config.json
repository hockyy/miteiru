{
  "win": {
    "icon": "resources/icon.ico",
    "target": ["nsis"],
    "artifactName": "miteiru-${version}-${os}-${arch}.${ext}",
    "extraResources": [
      {
        "from": "resources/live-captions",
        "to": "live-captions",
        "filter": ["**/*", "!**/.gitkeep"]
      }
    ]
  }
}
